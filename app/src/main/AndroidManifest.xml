<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectfile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projectfile"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".View.add_Pole"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
            <!--android:theme="@android:style/Theme.Dialog"--><!--주석 풀면 팝업-->
        <activity
            android:name=".View.Main"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".MainActivity2" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".View.Thread"
            android:exported="false" />
        <activity
            android:name=".View.Notice_alarm"
            android:exported="false" />
        <activity
            android:name=".View.Popup"
            android:exported="false"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".View.Login"
            android:exported="false" />
        <activity
            android:name=".View.update_Pole"
            android:exported="false" />
        <activity
            android:name=".View.Pole_detail"
            android:exported="false"
            android:theme="@android:style/Theme.Dialog"/>

        <activity
            android:name=".View.Start"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>